# 11 — HOW TO RUN WITH CLINE

Open this whole folder as the VS Code workspace so `.clinerules/` is active.

Use Cline in Act mode.

Send exactly:

> Read `09_CLINE_MASTER_PROMPT.md` and execute it autonomously to completion. The Blueprint is pre-approved. Do not stop for intermediate approval unless you encounter a Level-3 blocker.

Then let Cline continue through build and browser QA.

Do not manually rename source assets first.
The pack already uses normalized source filenames.
